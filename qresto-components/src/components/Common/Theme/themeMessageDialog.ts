import { createTheme } from '@mui/material/styles';

export const themeMessageDialog = createTheme({
    palette: {
        primary: {
            light: '#8C8C8C',
            main: 'black',
            dark: '#3E0808',
            contrastText: '#EEEAE2',
        },
    },
});